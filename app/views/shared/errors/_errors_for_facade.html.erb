<% objs_errors = object.errors.full_messages.uniq %>
<% objs_error.each do |err| %>
    <% objs_errors << object.errors(err.to_sym).full_messages.uniq %>
<% end %>
<div id="error_explanationx" class="alert  alert-danger">
  <a href="#" class="close" data-dismiss="alert">x</a>
  <h4><%= pluralize(objs_errors.flatten.count, 'error') %> prohibited this record from being saved:</h4>
  <ul>
    <% objs_errors.flatten.each do |msg| %>
        <li><%= msg %></li>
    <% end %>
  </ul>
</div>
